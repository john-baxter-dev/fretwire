# Capture: NN-<short-action>

- **pcap file:** `NN-<short-action>.pcapng`
- **date:**
- **HX Edit screen / context:** (e.g. Edit view, amp block selected)

## Action performed (exactly one)
- **What:** (e.g. turned the amp **Drive** knob)
- **Block / slot:** (e.g. DSP0, block 1, model `HD2_AmpGermanMahadeva`)
- **Parameter:** (e.g. `Drive`)
- **Before value (displayed):**
- **After value (displayed):**
- **Notes:** (anything else that happened — meters, flashes, etc.)

## Analysis notes (filled in later)
- Endpoint(s):
- Bytes of interest:
- Decoded meaning:
